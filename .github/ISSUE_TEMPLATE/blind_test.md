---
name: A test is blind
about: A rule that nothing actually pins, or a check that cannot fail
labels: evidence
---

**What is claimed** (a rule in `docs/SPEC.md`, a row in its conformance map, or a sentence in `docs/RESULTS.md`):

**Why nothing pins it:**

<!-- Examples of the shape this takes: an operation whose coverage count is near zero; an assertion whose tolerance is
     wider than the deviation it is supposed to catch; a figure in a document that no run records. -->

**The smallest test that would fail today if the rule were broken:**
